import fs from "node:fs";
// import { writeLargeImageFile } from "../../tool/file/base.js";
/**
 * 将请求上传的img写入指定文件夹下。
 */
export function postImg(res, query, post, filesr, prames) {
//   console.log(typeof prames);
}
